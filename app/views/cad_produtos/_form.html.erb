<%= form_with(model: @cad_produto, html: { multipart: true }, local: true) do |form| %>
  <% if @cad_produto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cad_produto.errors.count, "error") %> prohibited this @cad_produto from being saved:</h2>

      <ul>
      <% @cad_produto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :categoria, class: "w3-text-blue"%>
    <%= form.text_field :categoria, class: "w3-border w3-light-grey" %>
  </div>

  <div class="field">
    <%= form.label :produto, class: "w3-text-blue" %>
    <%= form.text_field :produto, class: "w3-border w3-light-grey" %>
  </div>

  <div class="field">
    <%= form.label :caracteristica, class: "w3-text-blue" %>
    <%= form.text_area :caracteristica, class: "w3-border w3-light-grey" %>
  </div>

  <div class="field">
    <%= form.label :imagem, class: "w3-text-blue" %>
    <%= form.file_field :imagem, class: "w3-btn w3-white"%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
